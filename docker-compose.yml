version: "3.5"

services:
  app:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    entrypoint: ./docker/entrypoint.sh
    ports:
      - "${APP_PORT}:8000"
    volumes:
      - "./:/app"
    networks:
      - "dants-api"
    depends_on:
      - "db"
  db:
    image: "mysql"
    ports:
      - "${MYSQL_PORT}:3306"
    volumes:
      - "db:/data/db"
    networks:
      - "dants-api"

networks:
  dants-api:

volumes:
  db:
